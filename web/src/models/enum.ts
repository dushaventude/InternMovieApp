export enum loadingStates {
    IDLE = 'idle',
    PENDING = 'pending',
    LOADING = 'loading',
    SUCCESS = 'success',
    FAILURE = 'failure'
  }
  
export enum pagination {
    PAGE_SIZE = 10
  }
  
  export enum responseStatus {
    SUCCESS = 'success',
    ERROR = 'error',
    WARNING = 'warning'
  }